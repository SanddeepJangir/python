class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        zero = 0
        
        for i in range(len(nums)):
            if nums[i]:
                nums[zero], nums[i] = nums[i], nums[zero]
                zero += 1  
