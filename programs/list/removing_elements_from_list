# we can remove elements from a list using some functions:
# remove(): remove list occurence of an element , value must exists otherwise gives ValueError
a = [1,2,3,5]
a.remove(2)
print(a)

# pop(): remove element at specific index, IndexError if indexx is invalid
# default index = last element
b = [1,2,4,9,65,7,8]
b.pop(0)
print(b)

# if you don't specify the index, the pop() method removes the last item
c = [1,2,3,4,5,6]
c.pop()
print(c)

# del(): it also remove the specified index
d = [1,34,6,77,89]
del d[2]
print(d)

# del keyword can also delete the list completely
e = [2,3,4,5,6,7]
del e   