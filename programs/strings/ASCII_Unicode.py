# ASCII: it uses no. 0 to 127 to represent characters.
print(ord("A"))
print(ord("a"))

# unicode: it supports characters from all languages + emojis.
# python uses unicode, so it supports every character. 
print(chr(65))
print(chr(5678))