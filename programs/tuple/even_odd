# Separate even and odd numbers.
t = (1, 2, 3, 4, 5, 6)
even = []
odd = []
for i in t:
    if i % 2 == 0:
        even.append(i)
    else:
        odd.append(i)
print("Even:", tuple(even))
print("Odd:", tuple(odd))